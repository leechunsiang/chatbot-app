import React from 'react';
import ReactDOM from 'react-dom/client';
import TabsDemo from './TabsDemo';
import './index.css';

ReactDOM.createRoot(document.getElementById('tabs-demo')!).render(
  <React.StrictMode>
    <TabsDemo />
  </React.StrictMode>
);
